# 腾讯云 COS 自动同步（可选）
# 归档同步脚本 scripts/sync-archive.mjs 默认只生成 archive/ archive.json；
# 若要自动上传到桶，请在本地自行创建 .env（已被 gitignore，不会提交）并填入：
# COS_SECRET_ID=你的SecretId
# COS_SECRET_KEY=你的SecretKey
# 密钥在腾讯云「访问管理 CAM → API 密钥管理」创建；建议使用子账号并仅授予该桶读写权限

# 漫画维护页管理员口令（仅在 Vercel 等服务端环境设置，不要添加 VITE_ 前缀）
# DOUJIN_PASSWORD=请在部署平台中设置
