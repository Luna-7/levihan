{
  "name": "levihan",
  "private": true,
  "workspaces": [
    "vercel-version"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=vercel-version",
    "build": "npm run build --workspace=vercel-version",
    "lint": "npm run lint --workspace=vercel-version",
    "test": "npm run test --workspace=vercel-version"
  }
}
